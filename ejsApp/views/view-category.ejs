<html>
  <head>
    <%-include('__bootstrap.ejs')%>
  </head>
  <body>
    <%-include('__header.ejs')%>
    <div class="container mt-2 p-0" style="height: 100vh">
      <% if (categoryList && categoryList.length > 0) { %>
      <table class="table m-0">
        <thead>
          <tr>
            <th>Category Id</th>
            <th>Category Name</th>
            <th>Edit</th>
            <th>Delete</th>
          </tr>
        </thead>
        <tbody>
          <% for(let index in categoryList){ %>
          <tr>
            <td><%= categoryList[index].category_id %></td>
            <td><%= categoryList[index].category_name %></td>
            <td>
              <a
                onclick="return confirm('Do you want to delete?')"
                href="/category/delete-category/<%= categoryList[index].id %>"
              >
                <button class="btn btn-outline-danger" style="width: 100px">
                  Delete
                </button>
              </a>
            </td>
            <td>
              <a href="/category/edit-category/<%= categoryList[index].id %>">
                <button class="btn btn-primary" style="width: 100px">
                  Edit
                </button>
              </a>
            </td>
          </tr>
          <% } %>
        </tbody>
      </table>
      <% } else { %>
      <div class="col-12 text-center">
        <h5 class="text-muted">No Category available.</h5>
      </div>
      <% } %>
    </div>
  </body>
</html>

<!-- <html>
  <head>
    <%- include('__bootstrap.ejs') %>
  </head>
  <body>
    <div class="container">
      <div class="row">
        <% if (categoryList && categoryList.length > 0) { %> <% for (let index
        in categoryList) { %>
        <div class="col-md-3 p-2">
          <div
            class="product d-flex flex-column align-items-center justify-content-center"
            style="box-shadow: 10px 10px 10px grey; height: 120px"
          >
            <h6><%= categoryList[index].category_id %></h6>
            <h4 class="text-success">
              <%= categoryList[index].category_name %> Rs.
            </h4>
            <div
              class="d-flex flex-row justify-content-around"
              style="width: 100%"
            >
              <a
                onclick="return confirm('Do you want to delete?')"
                href="/product/delete/<%= categoryList[index].id %>"
              >
                <button class="btn btn-outline-danger" style="width: 100px">
                  Delete
                </button>
              </a>
              <a href="/product/edit/<%= categoryList[index].id %>">
                <button class="btn btn-primary" style="width: 100px">
                  Edit
                </button>
              </a>
            </div>
          </div>
        </div>
        <% } %> <% } else { %>
        <div class="col-12 text-center">
          <h5 class="text-muted">No category available.</h5>
        </div>
        <% } %>
      </div>
    </div>
  </body>
</html> -->
